#include <iostream>
#include <bits/stdc++.h>
#include <cmath>

class Exp{
    private:
        int e;
    public:
        Exp(int x):e(x){}
        int operator()(int y){
            return pow(y,e);
        }
};

int Restar(int a){
    return a*2;
}


int main()
{
        int arr[] = {1, 2, 3, 4};
        Exp e(3);
        
        int(*q)(int);
        q = &Restar;
        
        std::transform(arr, arr+4, arr, Restar); ///toma el array, usa la funcion para cada dato del array y la ejecuta con esos datos como parámetro y la gaurda en el mismo o en otro arrya(en este caso, en el mismo array)
        for(int* p = arr; p<arr+4;p++){
            std::cout<<*p<<", ";
        }
        
        
    return 0;
}
