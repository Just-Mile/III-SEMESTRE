#include <iostream>

class MiClase{
    public:
    char x;
    char y;
    void imprimir(char z){
        std::cout<<x<<", "<<z<<"\n";
    }
};

int main()
{
    int(*q)(int, int); //puntero a funcion
    q = &mayorque;
    std::cout<<q(3, 6)<<"\n";
    
    
    void(MiClase::*r)(char); //puntero a método
    r = &MiClase::imprimir;
    //int* arr[3] //array de punteros
    MiClase obj; //para usar el puntero a método: Necesitamos instancia o puntero de la clase (MiClase)
    obj.*r('d');//contenido del puntero a mtodo
    
    int t=3;
    int(*arr[t])(int, int)={&mayorque, &menorque, &mayorqueabs};
    q = mayorque;
    int tam = 5;
    int A[]={3, -1, 2, 6, -4};
    BubbleSort(A, tam, arr[2]);
    for(int*p=A;p<A+tam;p++){
        std::cout<<*p<<", ";
    }
    std::cout<<"\n";
    for(int(*(r))(int, int)=arr;r<arr+t;r++){
        
    }
    
    return 0;
}
